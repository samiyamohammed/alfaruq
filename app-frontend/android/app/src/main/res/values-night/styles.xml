<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- 1. For Android 11 and below: 
             This uses the launch_background.xml drawable we made black -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- 2. For Android 12 and above (Modern Splash Screen API):
             These lines ensure the system-generated logo is hidden and the background is black -->
        <!-- Sets the splash background to black -->
        <!-- Sets the icon to transparent so it doesn't appear before your video -->
        <!-- Ensures the splash stays until Flutter is ready -->
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This determines the color behind your UI while it is running. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
